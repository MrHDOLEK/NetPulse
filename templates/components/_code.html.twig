{% macro code(code = '', class = '') %}
    <pre class="overflow-x-auto rounded-lg border border-line bg-bg p-4 font-mono text-[12.5px] leading-relaxed text-dim{{ class ? ' ' ~ class : '' }}">{{ code }}</pre>
{% endmacro %}
