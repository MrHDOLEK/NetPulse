[NetPulse] {{ probe }}/{{ connection }} recovered

Connection "{{ connection }}" on probe "{{ probe }}" recovered: {{ reason }}.

Latest measurement:
  download:    {{ downloadBits }} bps
  upload:      {{ uploadBits }} bps
  ping:        {{ pingMs }} ms
  packet loss: {{ packetLoss }} %
