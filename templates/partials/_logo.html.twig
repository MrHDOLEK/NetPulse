{% set size = size|default(30) %}
<span class="grid shrink-0 place-items-center rounded-[9px] bg-accent" style="width: {{ size }}px; height: {{ size }}px">
    <svg width="{{ (size * 0.62)|round }}" height="{{ (size * 0.62)|round }}" viewBox="0 0 24 24" fill="none"
         stroke="oklch(0.2 0.02 250)" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M2 13h4l2-7 4 14 3-9 2 4h5"></path>
    </svg>
</span>
